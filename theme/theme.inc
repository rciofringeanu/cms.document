<?php

/**
 * Default renderer for the instruments table inside the documents profile page
 * @param $variables
 * @return string
 */
function theme_table__documents_template_instrument($variables) {
    return theme_table($variables);
}

function theme_table__documents_template_country($variables) {
    return theme_table($variables);
}
